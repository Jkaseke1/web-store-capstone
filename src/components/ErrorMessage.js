// ErrorMessage.js
// This file contains a component for displaying an error message

import React from 'react';

const ErrorMessage = ({ message }) => {
  return (
    <div>Error: {message}</div>
  );
};

export default ErrorMessage;